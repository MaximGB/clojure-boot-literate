#+SETUPFILE: ../build/setup.org

* Пространство имён Core

** Функции

*** -main

    #+NAME: core/main
    #+BEGIN_SRC clojure :tangle (org-sbe in-clj-path (path \"core.clj\"))
      (ns <<get-specification-param(title="Имя проекта")>>.<<get-specification-param(title="Пространство имён main")>>
        (:gen-class))

      (defn -main
        "Project's entry point"
        [& args]
        (println "Hello, from <<get-specification-param(title="Имя проекта")>>!"))
    #+END_SRC

    #+NAME: core/main
    #+BEGIN_SRC clojure :tangle (org-sbe in-cljs-path (path \"core.cljs\"))
      (ns <<get-specification-param(title="Имя проекта")>>.<<get-specification-param(title="Пространство имён main")>>
        (:refer-clojure))

      (enable-console-print!)

      (defn ^:export -main
        "Project's entry point"
        [& args]
        (println "Hello, from <<get-specification-param(title="Имя проекта")>>!"))
    #+END_SRC

    #+NAME: index.html
    #+BEGIN_SRC html :tangle (org-sbe in-resources-path (path \"index.html\"))
      <!doctype html>
      <html>
       <head>
        <title><<get-specification-param(title="Имя проекта")>></title>
       </head>
       <body>
         <script src="main.js"></script>
       </body>
     </html>
  #+END_SRC
